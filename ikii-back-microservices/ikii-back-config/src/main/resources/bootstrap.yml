server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ikii-developer/ikii-config-server.git
          skipSslValidation: true          
          username: arturo199
          password: Ajedrez94#

eureka:
  client:
    region: default
    registryFetchIntervalSeconds: 10
    serviceUrl:
      defaultZone: ${EUREKA:http://127.0.0.1:8761/eureka/}
