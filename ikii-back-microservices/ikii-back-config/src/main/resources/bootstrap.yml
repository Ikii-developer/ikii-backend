server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: file:/home/mentive/1SpringToolSuiteWorkspace/ikii-mvp/ikii-config-server-master

eureka:
  client:
    region: default
    registryFetchIntervalSeconds: 10
    serviceUrl:
      defaultZone: http://registry:8761/eureka/
